int main
{
    if(1)
    {  // this is a comment
        int a;  // here a is only defined inside of the if's scope
        a = 3;
    }
    int x = a + 2; // and so the a is undeclared over here outside of the scope
    return x;
}